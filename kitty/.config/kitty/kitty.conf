# Use the login shell (set via chsh), not a hardcoded path
shell .

# Color theme — swap by repointing the theme.conf symlink to any file under
# kitty-themes/themes/ (https://github.com/dexpota/kitty-themes).
include theme.conf
